# =============================================================================
# News Digest Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Anthropic API key (optional if using `claude login` for Pro subscription)
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# SMTP Settings
# =============================================================================
# Gmail example - you MUST use an App Password, not your regular password
# Get one at: https://myaccount.google.com/apppasswords

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx

# Optional: separate From address (defaults to SMTP_USER)
# SMTP_FROM=digest@yourdomain.com

# =============================================================================
# Digest Settings
# =============================================================================

# Recipients (comma-separated, no spaces)
DIGEST_EMAIL=you@example.com,friend@example.com

# Optional: display name for From header and email subject
# Use quotes if the name contains special characters (apostrophes, etc.)
DIGEST_NAME="Your Daily Digest"
