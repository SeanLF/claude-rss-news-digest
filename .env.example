# Anthropic API key (optional if using `claude login` for Pro subscription)
# ANTHROPIC_API_KEY=sk-ant-...

# SMTP settings (Gmail example - use App Password, not regular password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-app-password
# SMTP_FROM=digest@yourdomain.com  # Optional: separate From address

# Digest settings
DIGEST_EMAIL=you@example.com,friend@example.com
DIGEST_NAME=Your Daily Digest  # Display name for From header and Subject
