services:
  news-digest:
    build: .
    environment:
      - ANTHROPIC_API_KEY
      - RESEND_API_KEY
      - RESEND_FROM
      - DIGEST_EMAIL
      - DIGEST_NAME
    volumes:
      - ./data:/app/data
