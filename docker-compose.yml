services:
  news-digest:
    build: .
    environment:
      - ANTHROPIC_API_KEY
      - SMTP_HOST
      - SMTP_PORT
      - SMTP_USER
      - SMTP_PASS
      - SMTP_FROM
      - DIGEST_EMAIL
      - DIGEST_NAME
    volumes:
      - ./data:/app/data
