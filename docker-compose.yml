services:
  news-digest:
    build: .
    environment:
      - ANTHROPIC_API_KEY
      - SMTP_HOST
      - SMTP_PORT
      - SMTP_USER
      - SMTP_PASS
      - DIGEST_EMAIL
    volumes:
      - ./data:/app/data
